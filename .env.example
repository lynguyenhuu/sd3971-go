# Database Configuration
# When using docker-compose, set DB_HOST=postgres
# When running locally, set DB_HOST=localhost
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=products_db

# Application Configuration
APP_PORT=8080
